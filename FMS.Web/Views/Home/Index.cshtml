<div class="text-center">
    <h1> Special Fleet Management System</h1> 
    @ViewBag.Message <b>@ViewBag.LongTime</b>
</div>

<div class="callout callout-info">
    <h1>Home Page</h1>
    <p class="text-center"> Today is <b> @DateTime.Now.ToLongDateString() </b> </p>
</div>

<div asp-condition=@(User.IsInRole("admin") || User.IsInRole("manager")) class="callout callout-warning text-center">
    Only visible when logged in
</div>



